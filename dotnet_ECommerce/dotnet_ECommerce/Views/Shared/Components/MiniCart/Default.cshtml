@model IEnumerable<dotnet_ECommerce.Models.CartItems>

<div class="row">
    <h4>
        My Cart
    </h4>
    <table class="table">
        <thead class="text-center">
            <tr>
                <th>
                    Item
                </th>

                <th>
                    Name
                </th>
                <th>
                    Quantity
                </th>
                <th>
                    Total
                </th>
            </tr>
        </thead>
        @if (@Model.Count() == 0)
        {
            <tbody>
                <tr class="text-center">
                    <td colspan="4">
                        Shopping Cart Is Empty
                    </td>
                </tr>
            </tbody>
        }
        else
        {
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="text-center">
                        <td>
                            <img src="@Html.DisplayFor(modelItem => item.Product.Image)" width="75" height="75"/>
                        </td>
                        <td>
                            @item.Product.Name
                        </td>
                        <td>
                            @item.Quantity
                        </td>
                        <td>
                            $ @(item.Quantity * item.Product.Price)
                        </td>
                    </tr>
                }
            </tbody>
        }
    </table>
</div>