@page
@using Microsoft.AspNetCore.Identity
@using dotnet_ECommerce.Models
@model dotnet_ECommerce.Pages.Shop.CartModel
@inject SignInManager<ApplicationUser> SignInManager
@{
    ViewData["Title"] = "My Cart";
    Layout = "_Layout";
}
<div class="container text-center">
    <h1 class="text-left">
        My Cart
    </h1>
    <div class="container">
        <div class="row">
            @*@for (int i = 0; i < Model.CartItem.ToList().Count; i++)
            {
                <div class="col-sm">
                    <img class="detail-img" src="@Model.CartItem.ElementAt(i).Product.Image" width="350" />
                </div>
                <div class="col-sm">
                    <div class="row">
                        <h6>@Model.CartItem.ElementAt(i).Product.Name</h6>
                    </div>
                    <form method="post">
                        <div>
                            <label asp-for="@Model.CartItem.ElementAt(i).Quantity">Quantity&nbsp;&nbsp;</label>
                            <input asp-for="@Model.CartItem.ElementAt(i).Quantity" type="text">
                            <p>&nbsp;</p>
                        </div>
                    </form>
                    <div class="row">
                        <h6>$@Model.CartItem.ElementAt(i).Product.Price</h6>
                    </div>
                </div>
                <div>
                    <a asp-action="Edit" asp-route-id="@Model.CartItem.ElementAt(i).ProductID">Update</a>
                    <a asp-action="Delete" asp-route-id="@Model.CartItem.ElementAt(i).ProductID">Remove</a>
                </div>
            }*@

            @foreach (var item in Model.CartItem)
            {
                <div class="col-sm">
                    <img class="detail-img" src="@item.Product.Image" width="350" />
                </div>
                <div class="col-sm">
                    <div class="row">
                        <h6>@item.Product.Name</h6>
                    </div>
                    <form method="post">
                        <div>
                            <label asp-for="@item.Quantity">Quantity&nbsp;&nbsp;</label>
                            <input asp-for="@item.Quantity" type="text">
                            <p>&nbsp;</p>
                        </div>
                    </form>
                    <div class="row">
                        <h6>$@item.Product.Price</h6>
                    </div>
                </div>
                <div>
                    <a asp-action="Edit" asp-route-id="@item.ProductID">Update</a>
                    <a asp-action="Delete" asp-route-id="@item.ProductID">Remove</a>
                </div>
            }
            <form method="post">
                <button class="add-to-cart-button">Check Out</button>
            </form>
        </div>
    </div>
</div>
